{% extends 'base.html.twig' %}

{% block title %}{% trans %}My space{% endtrans %} - LocationImmo
{% endblock %}

{% block body %}
	<aside id="fh5co-hero" class="js-fullheight">
		<div class="flexslider js-fullheight">
			<ul class="slides">
				<li style="background-image: url({{asset('assets/images/properties/home_04.jpg')}})">
					<div class="overlay"></div>
					<div class="container-fluid">
						<div class="row">
							<div class="col-md-8 col-md-offset-1 text-center js-fullheight slider-text">
								<div class="slider-text-inner">
                  <div class="global-form">
                    <h1 class="text-green">{% trans %}Register{% endtrans %}</h1>
                    {% for flashError in app.flashes('verify_email_error') %}
                      <div class="alert alert-danger" role="alert">{{ flashError }}</div>
                    {% endfor %}
                    {{ form_start(registrationForm) }}
                      <div class="row">
                        <div class="col-sm-6">
                          <div class="form-group">
                            {{ form_row(registrationForm.firstname, {label: 'Firstname'|trans}) }}
                          </div>
                        </div>
                        <div class="col-sm-6">
                          <div class="form-group">
                            {{ form_row(registrationForm.lastname, {label: 'Lastname'|trans}) }}
                          </div>
                        </div>
                      </div>
                      <div class="form-group">
                        {{ form_label(registrationForm.email) }}
                        {{ form_errors(registrationForm.email) }}
                        {{ form_widget(registrationForm.email) }}
                      </div>
                      <div class="form-group">
                        {{ form_row(registrationForm.plainPassword, {label: 'Password'|trans}) }}
                      </div>
                      <div class="form-group">
                        {{ form_row(registrationForm.roles, {label: 'My profile'|trans}) }}
                      </div>
                      <div class="form-group">
                        <div class="checkbox-group">
                          {{ form_widget(registrationForm.agreeTerms) }}
                          {{ form_label(registrationForm.agreeTerms) }}
                        </div>
                      </div>
                      <div class="button-right">
                        <button type="submit" class="btn btn-primary">{% trans %}Register{% endtrans %}</button>
                      </div>
                    {{ form_end(registrationForm) }}
								  </div>
                </div>
							</div>
						</div>
					</div>
				</li>
			</ul>
		</div>
	</aside>
{% endblock %}
